{% extends 'base.html.twig' %}

{% block title %}UploadedImage{% endblock %}

{% block body %}
    <h1>UploadedImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ uploaded_image.id }}</td>
            </tr>
            <tr>
                <th>Image_url</th>
                <td>{{ uploaded_image.imageUrl }}</td>
                <tr>
                    <th>Image</th>
<td><img src="{{ asset('uploads/images/' ~ uploaded_image.imageUrl) }}" alt="Uploaded Image" style="max-width: 100%; height: auto;"></td>
                </tr>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_uploaded_image_index') }}">back to list</a>

    <a href="{{ path('app_uploaded_image_edit', {'id': uploaded_image.id}) }}">edit</a>

    {{ include('uploaded_image/_delete_form.html.twig') }}
{% endblock %}
