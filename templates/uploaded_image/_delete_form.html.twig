<form method="post" action="{{ path('app_uploaded_image_delete', {'id': uploaded_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ uploaded_image.id) }}">
    <button class="btn">Delete</button>
</form>
